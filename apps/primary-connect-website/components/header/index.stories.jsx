import React from 'react';
import { Header } from './index';

export default {
  component: Header,
  title: 'Header',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props = {};

  return <Header />;
};
